# Implementation Plans

Generated by the `improve` skill on 2026-08-21, following a Tech Debt & Architecture
audit of the Flutter app (all 5 findings selected for planning). Execute in the
order below unless dependencies say otherwise. Each executor: read the plan
fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Consolidate duplicated duration/time formatters | P1 | S | — | TODO |
| 002  | Standardize action-error handling | P2 | M | — | TODO |
| 003  | Split analytics/providers.dart into per-insight files | P2 | M | — | TODO |
| 004  | Document the data-layer convention | P3 | S | 003 | TODO |
| 005  | Decompose the three largest screen files | P3 | L | 003 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- 004 depends on 003 because the CLAUDE.md paragraph it writes describes which features have a `data/` folder — `analytics` only gets one once 003 lands, so 004 should follow it to describe the post-split state accurately.
- 005 depends on 003 because `stage_radar_screen.dart` (one of the three files it decomposes) consumes providers that 003 relocates from `analytics/providers.dart` into `analytics/data/*.dart` — doing 005 first would mean immediately re-touching import paths that 003 changes again.
- 001 and 002 have no dependencies and can run in either order or in parallel with 003.

## Findings considered and rejected

None — all 5 findings from the audit were selected for planning by the operator.
